core:
  measurements:
    readMode: interval
    readIntervalSensors: 900
    wateredThreshold: 20

interface:
  vad:
    sampleRate: 16000
    chunk: 1024
    speechPause: 2
  stt:
    device: cpu
  tts:
    device: cpu